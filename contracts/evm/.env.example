# Optional local RPC override for npm run deploy:local.
ETH_RPC_HTTP=http://127.0.0.1:8545

# Optional verification skip for npm run deploy:testnet.
SKIP_VERIFY=0

# Testnet deployer key is read from Hardhat vars, not this file:
# npx hardhat vars set PRIVATE_KEY
