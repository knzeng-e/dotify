# W02 — evidence and handoff

## Identity

- Sequence and scope: W02 — protect upload resources and session revocation.
- Date: 2026-09-07, updated 2026-09-08 for PR review feedback.
- Starting dev SHA: `dea7ba217b7d11b46b5a5bf5ff21890fb493d7ad`.
- Current dev base after Product catalog drift fix #140:
  `673d41d5411200645af4f282165c7e7160087719`.
- Implementation SHA actually tested:
  `98ab8ccbb0e89151bf0c37a01a3adcad319bffbc`.
- Branch / PR / issue: `feat/upload-session-boundaries`;
  https://github.com/knzeng-e/dotify/pull/139;
  https://github.com/knzeng-e/dotify/issues/138.
- Related dependency evidence:
  `docs/backlog/implementation/evidence/W01.md`; W01 is merged on `dev`.
- Code readiness: locally verified after addressing PR review comments and
  rebasing on #140; remote jobs are being retriggered from the updated branch.
- Release readiness: not deployed; live artist upload and Product host checks
  not run.

## Result and decisions

W02 replaces public upload endpoints with an artist capability flow. The web
and Product identity adapters first open an existing EIP-191 or Product
sr25519 session. The API resolves `ArtistDirectory.runtimeOf(requester)` and
only an address with a runtime can obtain a short-lived, single-use capability.
That capability is signed with a purpose-specific HKDF key and binds the
requester, chain, audio/cover/metadata purpose, maximum bytes, expiry, process
epoch, and unique ID.

The API reserves per-artist and global capacity when it issues the capability.
It enforces outstanding-grant concurrency and rolling completed-byte quotas,
detects supported media from received container bytes instead of browser MIME,
requires MP3 uploads to contain a complete sequence of MPEG audio frames rather
than a single plausible header, checks audio content identity, and releases
reservations on validation, storage, or interruption failure. Pinata requests
now receive the inbound abort signal. Manifest objects are strict at every
nested boundary.

Session and upload tokens include a random process epoch. Dotify's tracked Fly
topology is restricted to one API machine. Logout revocation remains effective
during that process; after a restart every earlier session and upload token is
rejected, so a logged-out token cannot become valid again. This deliberately
chooses safe restart invalidation over a new persistent datastore migration.
Horizontal scaling remains unsupported until quota and revocation state share
a transactional store. The frontend now clears a rejected stored session only
when local storage still contains the rejected token, so a delayed 401 from one
parallel upload cannot delete a freshly refreshed session created by another.

Free listening and guest room entry are unchanged and remain unauthenticated.
No contracts, keys, stored ciphertext, or pinned manifests are migrated.

## Verification

| Command or real scenario | Environment and build | Observed result | Artifact |
| --- | --- | --- | --- |
| `npm --prefix services/api ci` | Node 22.13.1 / npm 10.9.2, local isolated worktree | Passed; npm reported 0 vulnerabilities. | Terminal output |
| `npm --prefix web ci` | Node 22.13.1 / npm 10.9.2, local isolated worktree | Passed; npm reported the existing web dependency baseline of 28 vulnerabilities: 1 low and 27 high. | Terminal output |
| `npm --prefix services/api test` | Implementation SHA `23ef223`; Node test runner + tsx | Passed: 25 suites, 111 tests. | Terminal output |
| `npm --prefix services/api run typecheck` | Implementation SHA `23ef223`; TypeScript | Passed. | Terminal output |
| `npm --prefix web run test:unit` | Implementation SHA `23ef223`; Vitest | Passed: 47 files, 370 tests. | Terminal output |
| `npm --prefix web run build` | Implementation SHA `23ef223`; production Vite build | Passed; existing Rollup annotation, mixed import, and large chunk warnings remain. | Terminal output |
| `npm --prefix web run lint` | Implementation SHA `23ef223`; ESLint | Passed with 3 existing React hook warnings in `App.tsx` and `ArtistShell.tsx`. | Terminal output |
| `npm --prefix web run fmt:check` | Implementation SHA `23ef223`; Prettier | Passed. | Terminal output |
| `CATALOG_API_URL=http://127.0.0.1:9 npm --prefix web run build:product-devnet` | Implementation SHA `23ef223`; Product DevNet Vite build with network refresh disabled | Passed using the existing tracked bootstrap; same inherited Rollup/chunk warnings. | Terminal output |
| `npm --prefix web run test:e2e -- --workers=1` | Implementation SHA `23ef223`; Playwright Chromium with local Vite/signaling services | Passed: 17 tests, including artist publication, Classic unlock, and wallet-free guest room audio. | Terminal output |
| Parallel `npm --prefix web run test:e2e` followed by isolated autoplay rerun | Implementation SHA `23ef223`; 4 Playwright workers | Initial exact-SHA run passed 16/17; manual-start autoplay button timed out. Isolated rerun passed, and the full serial run then passed 17/17. | `web/test-results` was transient and is ignored |
| `node scripts/backlog-sync.mjs --check --offline` | Local worktree | Passed with inherited warnings: item `24` lacks an issue mapping and duplicate `08` docs exist. | Terminal output |
| `flyctl config validate -c services/api/fly.toml` | Fly CLI, implementation SHA `23ef223` | Passed; single-machine configuration is valid. | Terminal output |
| Strict Product fixture regeneration | `web/fixtures/product-devnet-catalog.json` against starting `dev` | Detected inherited drift: fixture has 3 releases while the tracked bootstrap has 7. Generated change was discarded; no unrelated snapshot was committed. | Dev run https://github.com/knzeng-e/dotify/actions/runs/34132958761 |
| `git diff --check` | W02 implementation tree | Passed. | Terminal output |
| PR #139 remote CI | GitHub Actions, branch head `396737090a1e4ed1f8ae159af141e51f33efc12a` | Workflow syntax, repository hygiene, web, signaling, API, EVM/ABI, Playwright, backlog, Netlify preview, and automated review passed. Product DevNet drift failed on the inherited 7-versus-3 snapshot mismatch. | https://github.com/knzeng-e/dotify/actions/runs/34139795056 |
| `npm --prefix services/api test -- --test-name-pattern uploads` | Review-fix SHA `98ab8cc`; Node test runner + tsx | Passed: 25 suites, 112 tests. Added coverage rejects the former single-MPEG-header false positive. | Terminal output |
| `npm --prefix services/api run typecheck` | Review-fix SHA `98ab8cc`; TypeScript | Passed. | Terminal output |
| `npm --prefix web run test:unit -- src/services/keyService.test.ts src/services/pinata.test.ts` | Review-fix SHA `98ab8cc`; Vitest | Passed: 2 files, 21 tests. Added coverage preserves a concurrently refreshed session and verifies upload retry clears only the rejected token. | Terminal output |
| `npm --prefix web run fmt:check` | Review-fix SHA `98ab8cc`; Prettier | Passed. | Terminal output |
| `npm --prefix web run build` | Review-fix SHA `98ab8cc`; production Vite build | Passed; inherited Rollup annotation, mixed import, and large chunk warnings remain. | Terminal output |
| `CATALOG_API_URL=http://127.0.0.1:9 npm --prefix web run build:product-devnet` | Review-fix SHA `98ab8cc`; Product DevNet Vite build with network refresh disabled | Passed after #140; no generated Product catalog drift. Same inherited Rollup/chunk warnings. | Terminal output |

## PR review follow-up

Automated review on PR #139 raised two valid issues:

- `services/api/src/services/mediaValidation.ts` accepted any buffer whose first
  bytes looked like an MPEG frame header. The fix now computes MPEG frame
  lengths and requires at least two complete consecutive frames, optionally
  followed by an ID3v1 tag. The previous test fixture was replaced with a
  multi-frame fixture, and the one-header case now fails with
  `UPLOAD_MEDIA_INVALID`.
- `web/src/services/pinata.ts` could clear a session that another concurrent
  upload had already refreshed after an API restart. The fix makes
  `clearStoredSession(address, expectedToken)` conditional and uses the
  rejected token on both upload authorization and content-key session retries.
  A delayed 401 no longer deletes a newer stored token.

## Compatibility and operations

- Supported devices, browsers, and Product host versions: unit/build coverage
  includes both EVM and Product sr25519 identity adapters; Playwright uses
  desktop Chromium. No real Product host/device version was exercised.
- New config/permissions and documented defaults: upload TTL, rolling quota,
  byte limits, and concurrency limits are documented in
  `services/api/.env.example`, the environment reference, and the deployment
  runbook. `services/api/fly.toml` sets `max_machines_running = 1`.
- Storage/key/contract migration and compatibility evidence: none required.
  Capability and session HMAC keys are independently derived from the existing
  backend-only `CONTENT_KEY_MASTER_SECRET`; no frontend secret was added.
- Deployment identifiers: none; deployment was not authorized or run.
- Rollback procedure and rehearsal evidence: revert the W02 commits and deploy
  API and frontend together. Rolling back only one side intentionally leaves
  artist uploads fail-closed because old clients cannot satisfy protected API
  routes and new clients cannot authorize against an old API. Rehearsal not
  run.
- Data collected, retention, and user controls: process-local capability IDs,
  address-scoped byte usage, and revoked session IDs live only until expiry or
  restart. Tokens, signatures, keys, and uploaded contents are not logged.

## Acceptance mapping

| Sequence criterion | Passed / failed / not run | Supporting evidence or exact blocker |
| --- | --- | --- |
| Unauthorized upload is rejected | Passed | Route tests require a session for capability issuance and a scoped capability for every upload. |
| Valid artist is authorized with EVM and Product identity preserved | Passed locally | API authority route test plus frontend Product signer/session tests; real wallets and live chain not run. |
| Per-principal and global byte/concurrency quotas | Passed | Authorization service and route tests cover principal/global bytes, principal/global outstanding grants, accounting, and retry after abort. |
| Spoofed type and oversize payload are rejected from received bytes | Passed | Audio-as-image spoof, media detection, multipart budget, and metadata body budget tests pass. |
| Interrupted or rejected upload releases resources | Passed locally | AbortError storage route test and lease tests prove reservation cleanup; live client disconnect not run. |
| Expired, wrong-purpose, and replayed authorization fail closed | Passed | Focused service and route tests pass. |
| Logout followed by restart rejects old token and permits fresh login | Passed | Session service test uses two process epochs with the same master secret. |
| Cross-instance revocation or enforced single-instance topology | Passed for supported topology | Fly config validates with one machine; multi-instance operation is explicitly unsupported and documented. |
| Logs exclude credentials, keys, and uploaded content | Passed by review | Bearer authorization headers and auth bodies use existing logger redaction; new code logs only a constant encryption failure message. |

## Remaining gates

- Remote PR checks were retriggered after rebasing on #140 and addressing PR
  review feedback. They should be treated as the merge gate once complete.
- Run one live artist publication with a real EVM wallet and one with a Product
  sr25519 signer against the deployed API, chain RPC, and Pinata. Confirm the
  accepted media types with representative real audio and covers.
- Deploy/restart testing is still required to observe a prior session fail with
  `SESSION_RESTARTED` and a new sign-in succeed in the actual single-machine
  environment.
- The media detector validates supported container structure and signatures;
  it is not malware scanning or content moderation.

## Next agent

- Next eligible sequence(s), based on merged prerequisites: W03 becomes
  eligible after W02 merges. W04, W06, and W10 are independently eligible from
  W01.
- Files/interfaces changed that the next agent must inspect:
  `services/api/src/routes/uploads.ts`,
  `services/api/src/services/uploadAuthorizations.ts`,
  `services/api/src/services/sessionTokens.ts`, `web/src/services/pinata.ts`,
  `web/src/services/keyService.ts`, and
  `docs/operations/deployment-configuration.md`.
- Decisions that must not be silently reversed: upload capacity requires
  signed artist authority; purpose and byte budgets are single-use; Product
  sr25519 remains first-class; guest rooms and Free listening remain unsigned;
  API scaling above one machine requires a shared transactional quota/session
  store.
- Exact command/scenario to reproduce a remaining issue:
  none currently known from local validation; use the PR #139 remote checks as
  the current merge gate.
- Project/metadata updates completed and any inaccessible fields: issue #138
  and draft PR #139 are assigned, labeled, and added to Project 5 with
  `Status=In Progress`, `Priority=P1`, `Track=Production spine`, `Phase=Now`,
  `Type=Work`, and the W02 backlog document. No active milestone exists and no
  known reviewer was requested.
