# W11 — evidence and handoff

## Identity

- Sequence and scope: W11 — prove the Product DevNet user journey.
- Date: 2026-09-13.
- Starting dev SHA: `dab4e67e7b94a87b59a0623afc8b6e1f1810a67b`.
- Implementation SHA actually tested: `09e615a58b3202175beb8bf778bb714cf99963a0`
  for the W11 harness and docs; the follow-up CI workflow/evidence metadata
  commit must be covered by PR checks.
- Branch / PR / issue: `feat/product-devnet-journey`; draft PR #153; Refs #85.
- Related dependency evidence: W03, W04, W05, and W06 evidence files are present
  under `docs/backlog/implementation/evidence/`.
- Code readiness: locally verified.
- Release readiness: blocked for Product-readiness claim until live Product
  host CDM unlock and room evidence are supplied.

## Result and decisions

W11 adds a read-only Product DevNet journey harness instead of hiding the
remaining live Product gates inside prose. The harness validates the local
Product build contract: current SDK/tool pins, Product DevNet chain ID and CDM
registry, DotNS domain, canonical public URL, API and signaling origins, gateway
configuration, empty browser secrets, generated CDM package addresses, deploy
CLI pin, and the fact that the tracked Product profile still defaults to the
viem runtime adapter until Product CDM writes are live-proven.

The same command can consume two live evidence exports:

- the existing browser-side Product CDM host smoke JSON after a funded Product
  host account signs and submits a Classic unlock;
- a deliberately small room JSON that records a Product host sharing a
  canonical `https://dotify-test01.dev-dot.li/#/rooms/<code>` link and an
  ordinary browser guest joining without an account and hearing audio.

Missing live inputs are reported as `blocked` or `not-run`, never as passed.
Payment submission, confirmation, and access verification remain separate
states. The harness exits non-zero only when static config or supplied evidence
is unsafe/inconsistent, so it can run in CI or local release prep without
requiring funds, private keys, or Product host approval prompts.

Official Product DevNet context was refreshed before implementation:

- Product DevNet was reset on 2026-09-08/2026-09-09, with the active CDM
  registry at `0x05662b3dbd5dd9f2ff92d67630477e84b0b37c1f`.
- Product DevNet remains Asset Hub `1000`, People `1004`, Bulletin `1010`, and
  EVM chain `420420417`; the `paseo` preset targets a different Next network.
- The local pins are current for the referenced toolchain:
  `@parity/product-sdk@0.27.0`, `@parity/product-sdk-host@0.19.1`,
  `@parity/product-sdk-descriptors@0.11.0`,
  `@parity/product-sdk-statement-store@0.6.9`, `polkadot-api@1.23.3`, and
  `@polkadot-community-foundation/polkadot-app-deploy@0.16.2`.

No SDK upgrade, deploy, contract write, live payment, or hidden viem fallback was
introduced. CASH remains unsupported for this path.

While preparing PR #153, the `claude-review` check failed before producing any
review comments because the workflow attempted to install
`code-review@claude-code-plugins` from an external marketplace that GitHub
enterprise policy now blocks. The workflow now uses a direct Claude review
prompt through the existing `anthropics/claude-code-action@v1` action, avoiding
the external marketplace dependency while preserving the automated review gate.

## Verification

| Command or real scenario | Environment and build | Observed result | Artifact |
| --- | --- | --- | --- |
| `node --test web/scripts/product-devnet-journey-harness.test.mjs` | macOS local, Node `v22.13.1`, W11 working tree | Passed: 6 tests. Covers env parsing, Product app version parsing, static pass plus missing-live blocked status, complete Product smoke plus room evidence, secret-key rejection, and retired registry failure. | Terminal output |
| `npm --prefix web run smoke:product-journey -- --json-out /tmp/dotify-w11-product-journey.json --md-out /tmp/dotify-w11-product-journey.md` | Same | Passed with release-blocking live status: 26 static gates pass, 0 fail, 1 blocked Product CDM unlock, 1 not-run room journey. Generated JSON and Markdown evidence artifacts. | `/tmp/dotify-w11-product-journey.json`, `/tmp/dotify-w11-product-journey.md` |
| `npm --prefix web run test:unit -- src/features/productHost/productCdmHostSmokeEvidence.test.ts src/features/productHost/productHost.test.ts src/features/runtime/runtimeWriterProvider.test.ts src/features/payments/paymentReadback.test.ts` | Same | Passed: 4 files, 37 tests. Re-verified Product host detection, smoke evidence hygiene, runtime writer fail-closed mapping, and payment readback behavior. | Terminal output |
| `npm --prefix web run smoke:devnet` | First sandbox run | Failed with `fetch failed` for all six probes because sandbox networking was unavailable. | Terminal output |
| `npm --prefix web run smoke:devnet` | Escalated network access, same working tree | Passed: chain `420420417`, live Asset Hub blocks, both configured contract bytecodes, Bulletin RPC response, and IPFS gateway response. | Terminal output |
| `npm --prefix web run build:product-devnet` | Same | Passed. Catalog bootstrap refresh could not fetch in the local environment and kept the existing checked-in bootstrap. Build emitted the inherited Rollup warnings for `@scure/base`, mixed dynamic/static `protectedAudio.ts`, and large chunks. | `web/dist-product/` ignored build output |
| `npm --prefix web run lint` | Same | Passed with 0 errors and 3 existing hook dependency warnings in unchanged files: `src/App.tsx:35`, `src/App.tsx:69`, and `src/views/ArtistShell.tsx:25`. | Terminal output |
| `npm --prefix web run fmt:check` | Before formatting | Failed only for the two new scripts. | Terminal output |
| `npx prettier --write scripts/product-devnet-journey-harness.mjs scripts/product-devnet-journey-harness.test.mjs` | Same, run from `web/` with Node 22 path | Passed; formatted the new scripts. | Terminal output |
| `npm --prefix web run fmt:check` | After formatting | Passed. | Terminal output |
| `node scripts/backlog-sync.mjs --check --offline` | Repo root | Passed with existing offline warnings: active items without GitHub issue mapping and duplicate numbered backlog docs for `08`. | Terminal output |
| `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/claude-code-review.yml"); puts "ok"'` | Repo root | Passed; the updated Claude review workflow is valid YAML. | Terminal output |

## Compatibility and operations

- Supported devices, browsers, and Product host versions: no real Product
  Desktop/Web/iOS host was exercised in this sequence. The harness records those
  surfaces explicitly as blocked or not-run until live evidence is supplied.
- New config/permissions and documented defaults: no new environment variable
  or host permission. A new npm script, `smoke:product-journey`, is documented
  in the Product DevNet deployment runbook.
- Storage/key/contract migration and compatibility evidence: no storage, key,
  ABI, or contract state change. The harness confirms the current CDM registry
  and generated package addresses but performs no write.
- Deployment identifiers: not deployed.
- Rollback procedure and rehearsal evidence: revert the W11 branch. No database,
  contract, key, or Product deployment rollback is required.
- Data collected, retention, and user controls: the harness reads repository
  config plus optional operator-provided JSON evidence. It rejects evidence that
  contains content keys, signatures, nonces, or tokens.
- CI/review operations: Claude automated review no longer depends on an external
  plugin marketplace blocked by enterprise policy.

## Acceptance mapping

| Sequence criterion | Passed / failed / not run | Supporting evidence or exact blocker |
| --- | --- | --- |
| Refresh official SDK/host documentation and installed versions | Passed | Official Product DevNet update, address reference, and SDK docs were reviewed; local package/tool pins match the current documented versions. |
| Inspect existing smoke evidence and adapter implementation without rebuilding delivered code | Passed | Product host, Product CDM adapter, writer provider, payment readback, and Product CDM evidence panel tests were inspected and re-run. |
| Exercise Product account connection, H160 binding, CDM package resolution, native payment forwarding, inclusion/finality, and entitlement readback | Partially passed | Existing unit tests cover connection/mapping/readback semantics and fail-closed behavior. Live Product payment and readback were not run because no authorized funded Product host transaction was requested in this sequence. |
| Retain receipts when readback is delayed or fails | Passed locally | Existing payment/readback tests and Product CDM smoke event checks were re-run; the journey harness requires supplied smoke evidence to include the payment/readback checks rather than inferring success. |
| Distinguish submitted, confirmed, and access-verified | Passed locally | Current code and tests keep those states separate; the harness gates Product CDM success on supplied access readback evidence. |
| Never resubmit payment because a read times out | Passed locally | The harness is read-only, and payment readback tests cover bounded polling without re-submission. |
| CASH remains unsupported until W16 | Passed | No CASH rail or conversion path was introduced. |
| Verify manifest permissions, API/gateway/TURN egress, Bulletin/DotNS config, canonical room links, fallbacks, and chain IDs | Partially passed | Static gates verify host mode, origins, HTTPS endpoints, registry, generated CDM addresses, app version, domain, and `devnet` chain environment. Live TURN/media and Product host permission prompts were not run. |
| Complete surface matrix for standalone desktop/mobile, Product Desktop/Web, and Product iOS | Passed as evidence scaffold, live incomplete | Harness output names every surface, build SHA, app version, status, and missing evidence. Real devices/hosts remain not-run or blocked. |
| Permission denial, unavailable host, wrong chain, identity mismatch, canceled payment, reverted payment, delayed readback, duplicate-click safety | Partially passed | Existing unit coverage handles host unavailable/mapping/readback failure and current UI state separation. Real Product host denial/cancel/revert scenarios were not exercised. |
| Demonstrate one real Product-signed unlock and entitlement readback with authorized credentials/funds | Not run | No live Product host transaction/funds were authorized. Harness reports this as `blocked`. |
| Product host shares canonical room link and ordinary browser guest joins without account connection | Not run | Harness accepts the required room JSON schema, but no live room evidence was supplied. |

## Remaining gates

- Run an explicit Product CDM write smoke build with a funded Product host
  account and export the Product CDM host smoke JSON.
- Record the Product host approval screenshot and Fly/API logs for the key
  request that follows the payment readback.
- Run a Product-host-to-ordinary-browser room smoke with a canonical
  `https://dotify-test01.dev-dot.li/#/rooms/<code>` link and audible guest
  result, then pass the room JSON into `smoke:product-journey`.
- Fill the standalone desktop/mobile and Product Desktop/Web/iOS surface matrix
  with real host/device version evidence before making a Product pilot claim.
- Keep #85 open; W11 supplies the harness and local/static evidence, not the
  full Product pilot proof.

## Next agent

- Next eligible sequence(s), based on merged prerequisites: W12 becomes eligible
  after W11 merges if Product-readiness limitations are carried forward
  honestly. W16 and W17 remain later Product-specific research/implementation
  tracks.
- Files/interfaces changed that the next agent must inspect:
  `web/scripts/product-devnet-journey-harness.mjs`,
  `web/scripts/product-devnet-journey-harness.test.mjs`,
  `web/package.json`, `docs/operations/product-devnet-deployment.md`, and
  `docs/index.html`.
- Decisions that must not be silently reversed: the tracked Product profile
  stays on viem until Product CDM live evidence passes; missing live evidence is
  blocked/not-run rather than inferred; room guests remain wallet-free; Product
  iOS in-app WebRTC is not claimed.
- Exact command/scenario to reproduce a remaining issue:
  `npm --prefix web run smoke:product-journey -- --smoke-json <product-cdm-host-smoke.json> --room-json <product-room-evidence.json> --md-out /tmp/dotify-product-journey.md --json-out /tmp/dotify-product-journey.json`
  after collecting live Product host evidence.
- Project/metadata updates completed and any inaccessible fields: draft PR #153
  created targeting `dev`, assigned to `knzeng-e`, labeled `documentation`,
  `tooling`, `product`, `testing`, `dotify-backlog`, and `product-sdk`, and
  added to Project 5 with `Status=In Progress`, `Priority=P1`, `Track=Product
  SDK`, `Phase=Now`, `Type=Work`, and
  `Backlog doc=docs/backlog/implementation/W11-product-devnet-journey.md`. No
  repository milestone exists and no reviewer was requested because ownership
  was not specified.

Implementation summary: W11 adds a deterministic, read-only Product DevNet
journey harness, documents how to feed live Product/CDM and room evidence into
it, and records that static Product DevNet configuration is currently coherent
while Product readiness remains blocked on live host evidence. Exact next step:
open the draft PR to `dev`, then run the real Product host smoke and attach its
JSON, screenshot, and Fly/API logs.
