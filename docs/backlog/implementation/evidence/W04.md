# W04 - evidence and handoff

## Identity

- Sequence and scope: W04, make purchased access and ownership claims truthful.
- Date: 2026-09-08.
- Starting dev SHA: `00a5b6aecb664e9499577afae9e61d0436e2b999`.
- Implementation SHA actually tested after the W03 merge refresh:
  `ccc431fedebe0007300c403f1e833af863adf759`.
- Branch / PR / issue: `fix/access-promise`; PR #144; issue #143.
- Related dependency evidence: W01 is merged and dev quality gates are active.
  W04 depends only on W01, and this branch was refreshed on top of merged W03
  (`5d6cb65bf7f9d0ac98cfb7b3be8826faa2e1f344`) so it is no longer dirty
  against the current `dev`.
- Code readiness: locally verified after resolving the W03/W04 backlog manifest
  conflict; PR CI must pass again on the pushed head.
- Release readiness: not deployed.

## Result and decisions

W04 keeps the current runtime semantics and fixes the product promise around
Classic access. A Classic payment is now described as an on-chain paid-access
record with no fixed expiry in the current runtime, not a perpetual media
availability guarantee. Full playback opens only when the payment transaction is
included and a fresh runtime read-back confirms both `musicAccHasPaid` and
`musicAccCanAccess` for the listener address.

The implementation also makes inactive releases fail closed in the browser,
updates Classic support receipts with amount, conditions, and recipient
disclosure, and removes wording that implied NFT ownership, SmartRuntime
ownership, artist policy control, and royalty beneficiaries were the same right.
Those facts are now documented and tested separately.

No retroactive rights migration, contract upgrade, or legal entitlement model was
introduced. Solidity behavior is unchanged except for tests and comments that
document the current boundary.

## Verification

| Command or real scenario | Environment and build | Observed result | Artifact |
| --- | --- | --- | --- |
| `npm --prefix web run fmt:check` | macOS local, Node 22.13.1, refreshed W03+W04 branch | Passed. All matched web files use Prettier style. | Terminal output |
| `npm --prefix contracts/evm run fmt:check` | macOS local, Node 22.13.1 | Passed. Solidity and Hardhat files use Prettier style. | Terminal output |
| `npm --prefix web run test:unit -- src/features/access/accessPromise.test.ts src/features/access/accessPolicy.test.ts src/features/payments/paymentReadback.test.ts src/hooks/useCatalog.cache.test.ts src/services/pinata.test.ts src/features/uploads/preparedAudioUpload.test.ts` | macOS local, Vitest, refreshed W03+W04 branch | Passed: 6 files, 41 tests. Confirms W04 access promise coverage still works with W03 release-scoped key caching and upload runtime revalidation. | Terminal output |
| `npm --prefix web run test:unit` | macOS local, Vitest, refreshed W03+W04 branch | Passed: 51 files, 393 tests. Includes access promise, inactive access, payment read-back failure, support summary, canonical release cache, and prepared upload runtime coverage. | Terminal output |
| `npm --prefix services/api test` | macOS local, Node test runner + tsx, refreshed W03+W04 branch | Passed: 27 suites, 131 tests. Confirms merged W03 API boundaries remain green with W04. | Terminal output |
| `npm --prefix services/api run typecheck` | macOS local, Node 22.13.1, refreshed W03+W04 branch | Passed. | Terminal output |
| `npm --prefix services/api run build` | macOS local, Node 22.13.1, refreshed W03+W04 branch | Passed. | Terminal output |
| `npm --prefix contracts/evm test` | macOS local, Hardhat, refreshed W03+W04 branch | Passed: 55 tests. Includes the W04 policy table and ownership/control separation tests. | Terminal output |
| `npm --prefix contracts/evm run generate:abis` | macOS local, Hardhat | Passed. Nothing to compile; generated 6 ABI modules and index. No unexplained generated diff was left. | Terminal output |
| `npm --prefix web run lint` | macOS local, ESLint, refreshed W03+W04 branch | Passed with 0 errors and 3 existing React hook dependency warnings in `App.tsx` and `ArtistShell.tsx`. | Terminal output |
| `npm --prefix web run build` | macOS local, Vite production build, refreshed W03+W04 branch | Passed. Vite reported existing chunk-size and protected-audio import warnings. | Terminal output |
| `npm --prefix web run build:product-devnet` | macOS local, Product DevNet Vite build, refreshed W03+W04 branch | Passed. Catalog bootstrap refresh could not fetch in the local environment and kept the existing bootstrap; bundle built successfully. | Terminal output |
| `npm --prefix web run test:e2e -- classic-unlock.spec.ts` | macOS local, Chromium via Playwright, refreshed W03+W04 branch | Passed: 1 test. Initial sandboxed run failed with `listen EPERM` on `127.0.0.1:8789`; rerun outside the sandbox passed. | Terminal output |
| `node scripts/backlog-sync.mjs --check --offline` | macOS local, Node 22.13.1, refreshed W03+W04 branch | Passed with existing warnings: active item `24` has no GitHub issue mapping and duplicate numbered backlog docs exist for `08`. | Terminal output |
| `git diff --check` | local branch diff after W03/W04 merge refresh | Passed. | Terminal output |

## Compatibility and operations

- Supported devices, browsers, and Product host versions: local Chromium e2e was run. Real Product host, mobile host, and audible-device checks were not run for W04.
- New config/permissions and documented defaults: none.
- Storage/key/contract migration and compatibility evidence: no migration. Current on-chain storage and payment semantics are retained. Contract tests document the existing active/inactive, mode, paid, NFT owner, runtime owner, and royalty beneficiary boundaries.
- Deployment identifiers: not deployed.
- Rollback procedure and rehearsal evidence: revert the W04 PR on `dev`; no live deployment or contract upgrade was performed.
- Data collected, retention, and user controls: no new persistent telemetry. Product CDM payment smoke metrics remain browser-local/debug evidence and now only report after runtime read-back.

## Acceptance mapping

| Sequence criterion | Passed / failed / not run | Supporting evidence or exact blocker |
| --- | --- | --- |
| Contract tests reproduce the policy table covering Free, Classic, Human free, active/inactive, previously paid, and runtime upgrades. | Passed | `contracts/evm/test/ArtistRuntime.test.ts`; `npm --prefix contracts/evm test` passed with 55 tests. |
| UI tests verify correct price, asset, duration/conditions, and recipient disclosure. | Passed | `web/src/features/access/accessPromise.test.ts`; `web/e2e/classic-unlock.spec.ts`; unit and targeted e2e passed. |
| Canceled and failed payments never show acquired access. | Passed with local-test limitation | Access maps are updated only after successful read-back. `paymentReadback.test.ts` covers `hasPaid=true` plus `canAccess=false` as a verification failure. A real wallet cancellation browser run was not performed. |
| Deactivated previously purchased tracks give honest feedback. | Passed | Contract policy table shows inactive paid Classic returns `hasPaid=true` and `canAccess=false`; UI inactive gate uses `actionType: 'none'` and no payment CTA. |
| Check wording on mobile and Product hosts. | Partially run | Product DevNet build passed. Real mobile and Product host visual/manual checks were not run. |

## Remaining gates

- Do not merge if the refreshed PR head is red; GitHub PR checks remain the CI
  source for the current branch head.
- Real Product host smoke was not run. W04 does not deploy or prove live host signing, real final settlement, or audible playback.
- Mobile visual wording was not checked on device.
- `npm --prefix web run build:product-devnet` could not refresh the Product
  catalog bootstrap because fetch failed locally; it kept the existing bootstrap
  and built successfully.
- ESLint still reports three existing hook dependency warnings outside W04 scope.

## Next agent

- Next eligible sequence(s), based on merged prerequisites: after W04 merges, W05 is next in the solo order. W06 and W10 are independently eligible after W01; W07 requires W03.
- Files/interfaces changed that the next agent must inspect: `web/src/features/access/accessPromise.ts`, `web/src/hooks/useCatalog.ts`, `web/src/features/payments/paymentReadback.ts`, `web/src/features/access/accessPolicy.ts`, `contracts/evm/test/ArtistRuntime.test.ts`, `docs/explanation/access-control-model.md`, and `docs/explanation/royalty-settlement.md`.
- Decisions that must not be silently reversed: a Classic payment record is not a perpetual media availability promise; success UI requires fresh runtime read-back; inactive releases deny playback for everyone; NFT owner, original artist, runtime owner, and royalty beneficiaries remain distinct.
- Exact command/scenario to reproduce a remaining issue: run `npm --prefix web run test:e2e -- classic-unlock.spec.ts` outside the sandbox if local port binding fails with `listen EPERM`.
- Project/metadata updates completed and any inaccessible fields: issue #143 and
  PR #144 were added to Project 5 with P1, Production spine, Now, Work, and
  backlog doc fields. PR #144 is draft/In Progress, assigned to `knzeng-e`,
  labeled `P1`, `frontend`, `contracts`, and `dotify-backlog`. No milestone
  exists. No reviewer was requested because ownership was not known.
